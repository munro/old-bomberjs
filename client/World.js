define(
    ['../geometry/geometry', './Map', ],
    function (options) {
    var world;
    var map;
    var walls = [];
    var players = [];
        
    world = {
        playerCanMove: function (object, point) {
        }
    };
    
    Bomber.canMove(obj, {x: 0, y: 2}
    
    return world;
});
